language: java
jdk:
  - openjdk8
after_success:
  - mvn jacoco:report coveralls:report

deploy:
  provider: heroku
  api-key:
    secure: $HEROKU_API_KEY
  app: lsp-admin

before_install:
  - chmod +x mvnw